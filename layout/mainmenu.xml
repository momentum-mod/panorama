<root>
	<styles>
		<include src="file://{resources}/styles/chaosstyles.css" />
		<include src="file://{resources}/styles/mainmenu.css" />
		<include src="file://{resources}/styles/controls/button.css" />
	</styles>
	<scripts>
		<include src="file://{resources}/scripts/mainmenu.js" />
	</scripts>

	<snippets>
		<snippet name="MainMenuMovieSnippet">
			<Panel>
				<!-- Source is set in script. -->
				<!-- Set what you want the movie to be using the data-type example 'nuke' the .webm is appended in script. -->
				<Movie class="mainmenu-movie MainMenuModeOnly" 
					id="MainMenuMovie"
					src=""
					sound=""
					controls="none"
					repeat="true"
					hittest="false">
				</Movie>
			</Panel>
		</snippet>
		<snippet name="MainMenuVanitySnippet">
			<Panel>
			</Panel>
		</snippet>
	</snippets>

	<ChaosMainMenu class="InitialDisplay MainMenuRootPanel" hittest="false" useglobalcontext="true" oncancel="MainMenu.OnEscapeKeyPressed();">
		<Panel id="MainMenuMovieParent">
			<!-- Movie added/removed dynamically when changing the visibility of the main menu -->
		</Panel>
		<!-- Background video -->
		<Panel id="MainMenuVanityParent">
			<!-- Vanity is not used but the engine needs this to exist -->
		</Panel>-
		<Panel id="MainMenuContainerPanel" class="mainmenu-container">
			<Panel id="MainMenuBackground" class="mainmenu-bg">
				<Image id="MainMenuBackgroundImage" src="file://{images}/bg_00.vtf" />
			</Panel>
			
			<Image id="MainMenuLogo" src="file://{images}/logo.vtf" />
		
			<Panel id="MainMenuInput">  <!-- The actual panel with input & focus -->
				<!-- This fills the entire width, is aligned to top and is used as the navbar -->
				<Panel id="MainMenuOptionList" class="mainmenu-nav-list-container">
					<Panel id="JsMainMenuNavBar" class="mainmenu-nav-list">
						<Button id="MainMenuNavBarPlay"
								class="mainmenu-nav-list-item MainMenuModeOnly"
								onactivate="GameInterfaceAPI.ConsoleCommand( 'ShowMapSelectionPanel' )">
							<Label text="PLAY"/>
						</Button>

						<Button id="MainMenuNavBarSettings"
								class="mainmenu-nav-list-item"
								onactivate="GameInterfaceAPI.ConsoleCommand( 'mom_settings_show' )">
							<Label text="SETTINGS"/>
						</Button>

						<Button id="MainMenuNavBarCredits"
								class="mainmenu-nav-list-item"
								onactivate="GameInterfaceAPI.ConsoleCommand( 'mom_credits_show' )">
							<Label text="CREDITS"/>
						</Button>

						<Button id="MainMenuNavBarQuit"
								class="mainmenu-nav-list-item"
								onactivate="GameInterfaceAPI.ConsoleCommand( 'quit' )">
							<Label text="QUIT"/>
						</Button>
					</Panel>
				</Panel>
			</Panel>
			
			<Panel id="MainMenuSlider">
				<Label class="horizontal-align-center vertical-align-center" text="&lt;" />
			</Panel>
		</Panel>

		<!-- Make sure these are at the bottom to get the z-order right -->
		<ChaosPopupManager id="PopupManager" onpopupsdismissed="" />
		<ContextMenuManager id="ContextMenuManager" />
		<ChaosTooltipManager id="TooltipManager" hittest="false" />

		<!-- Doesn't actually include the notifications, but we use its position to figure out where to show them -->
		<Panel id="SteamNotificationsPlaceholder" hittest="false" />
	</ChaosMainMenu>
</root>
